// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}


datasource db {
  provider = "mysql" 
  url      = "mysql://admin:uGp)oW~3GVjf8JO<47u~S4{g42!F@group10db.c1yye8a84gu7.us-east-1.rds.amazonaws.com:3306/group10db"
}

model User {
  song		String
  artist	String
  album    	String
  year 		String
  genre 	String
  duration	String
  posts		Post[]
}

model Post {
  id        Int     @id @default(autoincrement())
  title     String
  content   String?
  published Boolean @default(false)
  author    User    @relation(fields: [authorId], references: [id])
  authorId  Int
}

